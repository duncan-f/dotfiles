#!/bin/sh

eval "$(xres2var)"

[ -n "$(pidof dunst)" ] || dunst -font "Monospace 11" -lb "${color7}" -lf "${color0}" -lfr "${color15}" -nb "${background}" -nf "${foreground}" -nfr "${color8}" -cb "${color1}" -cf "${color15}" -cfr "${color9}" &

